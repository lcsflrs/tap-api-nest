export enum PixStatus {
  Pending = "pending",
  Paid = "paid",
  Expired = "expired",
  Failed = "failed",
}

export enum PixType {
  AddBalance = "add_balance",
  Ingress = "ingress",
  StoreSale = "store_sale",
}
